// test('', () => {});
import Comparator from '../../src/Utils/Comparator';

beforeAll(() => {
  const comparator = new Comparator();
})

test('Deve instanciar a instancia de Comparator', () => {
});
